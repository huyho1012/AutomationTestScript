package Project.Backend;

import org.openqa.selenium.WebDriver;

public class BackendHomePage {
    WebDriver driver;
    public BackendHomePage(WebDriver webDriver){
        driver = webDriver;
    }

    public void clickToVerifyBusinessMenu(WebDriver driver) {
    }
}
